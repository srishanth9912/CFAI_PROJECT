# Sorting Algorithm Comparison

A Flask web application to compare Bubble Sort and Merge Sort performance.

## How to Run
1. Install requirements: `pip install flask`
2. Run: `python app.py`
3. Open browser at `http://127.0.0.1:5000`

## Algorithms
- Bubble Sort: O(n²)
- Merge Sort: O(n log n)
